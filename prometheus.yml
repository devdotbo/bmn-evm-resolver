global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'bmn-monitor'
    environment: 'production'

scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'prometheus'

  # BMN Alice Service
  - job_name: 'bmn-alice'
    static_configs:
      - targets: ['alice:8001']
        labels:
          service: 'alice'
          role: 'initiator'
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s

  # BMN Bob-Resolver Service (unified coordinator and acceptor)
  - job_name: 'bmn-bob-resolver'
    static_configs:
      - targets: ['bob:8002']
        labels:
          service: 'bob-resolver'
          role: 'acceptor-coordinator'
    metrics_path: '/metrics'
    scrape_interval: 10s
    scrape_timeout: 5s

  # Redis monitoring removed